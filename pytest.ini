[pytest]
# Ensures pytest adds the project root to sys.path, so tests/ can
# import from model/ (e.g. "from model.arw import retry_with_backoff")
# without a ModuleNotFoundError.
rootdir = .
pythonpath = .